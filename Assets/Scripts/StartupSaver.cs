using UnityEngine;

public class StartupSaver : MonoBehaviour
{
    // Temporary only, planned to be deleted
    private void Update()
    {
        Screen.SetResolution(1920, 1080, FullScreenMode.Windowed, 60);
    }
}
